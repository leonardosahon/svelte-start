// import { ROUTES, authRoutes } from '$lib/routes';
// import { auth } from '$lib/stores/auth.js';

// Set a static session expired handler for Api
// ApiStatic.sessionExpiredHandler = auth.sessionExpired;

// Setup your load function as well here
